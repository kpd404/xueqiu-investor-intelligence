[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "xueqiu-investor-intelligence"
version = "0.1.0"
description = "Source-independent investor behavior intelligence system"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.14,<2",
    "fastapi>=0.115,<1",
    "openai>=1.68,<3",
    "playwright>=1.50,<2",
    "psycopg[binary]>=3.2,<4",
    "pydantic>=2.10,<3",
    "pydantic-settings>=2.7,<3",
    "sqlalchemy>=2.0.36,<3",
    "uvicorn[standard]>=0.34,<1",
]

[project.optional-dependencies]
dev = [
    "httpx>=0.28,<1",
    "pytest>=8.3,<9",
    "pytest-cov>=6,<7",
    "ruff>=0.9,<1",
]

[tool.setuptools.packages.find]
where = ["."]
include = [
    "ai*",
    "backend*",
    "collectors*",
    "config*",
    "contracts*",
    "database*",
    "intelligence*",
    "ingestion*",
    "pipeline*",
    "prompts*",
    "resolution*",
    "signal_engine*",
]

[tool.setuptools.package-data]
prompts = ["**/*.md"]

[tool.pytest.ini_options]
addopts = "-ra --strict-config --strict-markers"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
